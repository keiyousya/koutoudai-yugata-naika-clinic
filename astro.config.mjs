// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://keiyousya.github.io',
  base: '/koutoudai-yugata-naika-clinic',
});
