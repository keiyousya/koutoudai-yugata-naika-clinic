---
import { Image } from "astro:assets";
import doctorImage from "../assets/doctor.jpeg";
---

<section id="doctor" class="section doctor">
  <div class="container">
    <h2 class="section-title">院長紹介</h2>
    <div class="doctor-content">
      <div class="doctor-photo-wrapper">
        <Image src={doctorImage} alt="院長とスタッフ" width={720} class="doctor-photo" />
      </div>
      <div class="doctor-info">
        <div class="doctor-name">
          <span class="position">院長</span>
          <h3>田村　慧人<span class="name-kana">たむら けいと</span></h3>
        </div>

        <div class="doctor-career">
          <h4>経歴</h4>
          <ul>
            <li>東北大学医学部医学科 卒業</li>
            <li>独立行政法人地域医療機能推進機構仙台病院</li>
            <li>株式会社慧陽社（医療システムの開発・運営）</li>
            <li>勾当台夕方内科クリニック 開院</li>
          </ul>
        </div>

        <div class="doctor-qualifications">
          <h4>所属学会</h4>
          <ul>
            <li>日本内科学会</li>
          </ul>
          <ul>
            <li>日本超音波医学会</li>
          </ul>
        </div>

        <div class="doctor-message">
          <h4>ご挨拶</h4>
          <p>
            当院は、日中の受診が困難な現役世代の方々のために、夕方から夜間の時間帯（17時〜21時）に診療を行ってまいります。急な風邪や体調不良への対応はもちろん、高血圧や糖尿病といった慢性疾患の継続的な治療にも対応いたします。
          </p>
          <p>
            日中は忙しくて通院が途切れてしまったという方も、ライフスタイルに合わせた無理のない通院が可能です。些細な不調から日々の健康管理まで、地域のかかりつけ医としてお気軽にご相談ください。
          </p>
          <p>
            皆様の健やかな毎日をサポートできるよう、スタッフ一同努めてまいります。
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .doctor {
    background: var(--color-bg-blue);
  }

  .doctor-content {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 80px;
    align-items: start;
  }

  .doctor-photo-wrapper {
    border-radius: 4px;
    overflow: hidden;
    border: 1px solid var(--color-border);
  }

  .doctor-photo {
    display: block;
    width: 100%;
    height: auto;
  }

  .doctor-info {
    display: flex;
    flex-direction: column;
    gap: 28px;
  }

  .doctor-name {
    border-bottom: 1px solid var(--color-border);
    padding-bottom: 20px;
  }

  .position {
    display: inline-block;
    font-size: 0.9rem;
    color: var(--color-text-muted);
    font-weight: 500;
    margin-bottom: 6px;
    letter-spacing: 0.06em;
  }

  .doctor-name h3 {
    font-size: 1.4rem;
    color: var(--color-navy);
  }

  .name-kana {
    display: block;
    font-size: 0.9rem;
    font-weight: 400;
    color: var(--color-text-muted);
    margin-top: 4px;
  }

  .doctor-career h4,
  .doctor-qualifications h4,
  .doctor-message h4 {
    font-size: 0.9rem;
    color: var(--color-navy);
    margin-bottom: 12px;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--color-border);
    letter-spacing: 0.03em;
  }

  .doctor-career ul,
  .doctor-qualifications ul {
    display: flex;
    flex-direction: column;
    gap: 10px;
    padding-left: 16px;
  }

  .doctor-career li,
  .doctor-qualifications li {
    position: relative;
    padding-left: 16px;
    color: var(--color-text-secondary);
    font-size: 0.9rem;
  }

  .doctor-career li::before,
  .doctor-qualifications li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 10px;
    width: 4px;
    height: 4px;
    background: var(--color-navy-medium);
    border-radius: 50%;
  }

  .doctor-message p {
    color: var(--color-text-secondary);
    line-height: 1.85;
    margin-bottom: 14px;
    font-size: 0.9rem;
  }

  .doctor-message p:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 768px) {
    .doctor-content {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .doctor-photo-wrapper {
      width: 40%;
      max-width: 280px;
      margin: 0 auto;
    }

    .doctor-info {
      padding: 0 4px;
    }

    .doctor-name h3 {
      font-size: 1.3rem;
    }
  }
</style>
