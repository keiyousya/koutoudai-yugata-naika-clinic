name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "deploy"
  cancel-in-progress: false

jobs:
  # Frontend: GitHub Pages
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build frontend
        run: pnpm --filter frontend build
        env:
          NODE_ENV: production

      - name: Build timecard
        run: pnpm --filter timecard build
        env:
          NODE_ENV: production
          VITE_API_URL: https://koutoudai-timecard-api.tamurakeito.workers.dev

      - name: Copy timecard to frontend dist
        run: cp -r ./timecard/dist ./frontend/dist/timecard

      - name: Build manuals
        run: pnpm --filter manuals build
        env:
          MANUALS_PASSWORD: ${{ secrets.MANUALS_PASSWORD }}

      - name: Copy manuals to frontend dist
        run: cp -r ./manuals/dist ./frontend/dist/manuals

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/dist

  deploy-frontend:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build-frontend
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # Backend: Cloudflare Workers
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Deploy reservation API
        run: pnpm --filter backend run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Deploy timecard API
        run: pnpm --filter backend run deploy:timecard
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
